<!DOCTYPE html>
<html lang="en">

    <%- include('../partials/head', {title : 'Mi perfil: ' + userProfile.firstName}) %>
<body>
    <%- include('../partials/header') %> 
    <main class="main-general">

        <div class="userProfileCont">
            <div class="profileWelcome">
                <div class="profileImg">
                    <img src="/img/users/<%= userProfile.image != "" ? userProfile.image : "user-default.png" %>" alt="Imagen de perfil de <%= userProfile.firstName %>">
                </div>
                <h2 class="profileGreeting">Hola <%= userProfile.firstName %>!</h2>
            </div>
            <div class="profileInfo">
                <p class="infoTitles">Nombre Completo</p>
                <p class="infoData"><%= userProfile.firstName + ' ' + userProfile.lastName  %></p>
                <br>
                <p class="infoTitles">Dirección</p>
                <p class="infoData"><%= userProfile.address %></p>
                <br>
                <p class="infoTitles">E-mail</p>
                <p class="areas__info"><%= userProfile.email %></p>
                <br>
            </div>
        </div>
        <div class="profileEditCont">
           <a href="/users/edit/<%= userProfile.id %>" class="profileEdit">EDITAR</a>
           <a href="/users/logout" class="profileEdit">CERRAR SESIÓN</a>
       </div>


    </main>
    <%- include('../partials/footer') %> 
</body>
</html>